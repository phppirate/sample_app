<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Rails Tutorial Sample App", root_path, class: "navbar-brand" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav" >
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", help_path %></li>
        <% if signed_in? %>
          <li><%= link_to "Users", "#" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <span class="caret"></span></a>
            <ul id="fat-menu" class="dropdown-menu">
              <li><%= link_to current_user do %><span class="glyphicon glyphicon-user"></span> Profile<% end %></li>
              <li><%= link_to edit_user_path(current_user) do %><span class="glyphicon glyphicon-cog"></span> Settings<% end %></li>
              <li class="divider"></li>
              <li><%= link_to signout_path, title: "Sign out", method: :delete do %><span class="glyphicon glyphicon-ban-circle"></span> Sign out<% end %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign in", signin_path  %></li>
        <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>